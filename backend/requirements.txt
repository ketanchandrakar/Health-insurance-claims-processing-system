# --- Web layer ---
flask>=3.0
flask-cors>=4.0

# --- Models / validation ---
pydantic>=2.7
python-dotenv>=1.0

# --- Vision extraction ---
# Using Google Gemini 2.5 Flash-Lite (free tier: 1,000 req/day, no credit card)
# Get API key at: https://aistudio.google.com/apikey
google-generativeai>=0.8

# --- Observability (mirrors the in-app trace) ---
langfuse>=2.0

# --- Fuzzy name matching (Gate 2 / consistency.py) ---
thefuzz>=0.20

# --- Testing ---
pytest>=8.0